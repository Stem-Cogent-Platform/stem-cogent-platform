# Build Status

## Stage 1.3 reconciliation

| Task | Status before session | Non-production | Production | Evidence | Changes |
|---|---|---|---|---|---|
| 1.3.1 — VPC & Networking | Already implemented and applied | Confirmed done | Confirmed done | Module validation succeeded; `terraform test` passed (1/1); both remote states contain the 26 scoped VPC/network resources; targeted plans returned no changes; AWS reports an available `10.0.0.0/16` VPC, six subnets across `public`, `private-app`, and `private-data` in `eu-west-1a`/`eu-west-1b`, two available NAT Gateways, and one attached Internet Gateway in each account. | No Terraform or AWS resource change. Production working directory was locally reinitialized against its existing encrypted remote backend. |
| 1.3.2 — Security Groups | Already implemented and applied | Confirmed done | Confirmed done | Both remote states contain the 14 scoped groups/rules and targeted plans returned no changes. Direct EC2 inspection confirmed public ALB ingress only on TCP 80/443; ALB-to-frontend TCP 3000; ALB-to-API TCP 8000; data-layer ingress from the API group only on TCP 5432/6379; and the configured application egress in both accounts. | No Terraform or AWS resource change. |
| 1.3.3 — KMS Keys | Already implemented and applied | Confirmed done | Confirmed done | KMS module test passed (1/1), both remote states contain seven CMKs plus seven aliases, and targeted plans returned no changes (including key policies). Direct KMS checks found all seven environment aliases and rotation enabled with a 365-day period on every key in both accounts. | No Terraform or AWS resource change. |
| 1.3.4 — S3 Buckets | Already implemented and applied under the v2 inventory | Confirmed done | Confirmed done | Owner confirmed SC-DOC v2 inventory: eight application buckets including processed documents, with Terraform state separate. S3 module test passed (1/1); both remote states contain 63 S3 module resources and all eight application buckets; targeted plans returned no changes after refreshing public-access blocks, SSE-KMS, lifecycle, policies, versioning/Object Lock where configured; direct S3 inventory found all eight expected buckets in each account. | No Terraform or AWS resource change. |
| 1.3.5 — Secrets Manager | Staging used the former Anthropic/SendGrid contract; production definitions existed without values | Confirmed done with Groq/Resend | Confirmed done with Groq/Resend | Owner approved replacing Anthropic with Groq and SendGrid with Resend. Terraform, IAM, tests, and backend settings now use `sc/{env}/llm/groq/api-key` and `sc/{env}/email/resend/api-key`. Formatting/compile checks passed; Secrets Manager test passed (1/1); IAM tests passed (6/6). Create-only applies added both new definitions in each account; staging updated four worker IAM policies; the four old definitions were scheduled for recoverable deletion. Production database/Redis versions were created only after AWS/state checks proved no live production RDS/Redis service could be desynchronized. Direct batch checks confirmed all nine required values populated and non-placeholder in each account. Final scoped plans returned no changes in both environments. | AWS: four new definitions created; four old definitions scheduled for deletion under the 30-day recovery window; four staging IAM policies updated in place; two generated production credential versions created. No raw value exposed. |
| 1.3.6 — RDS PostgreSQL | Staging complete; production absent | Confirmed done | Confirmed done | RDS module test passed (1/1), including explicit replica encryption/autoscaling assertions. Staging targeted plan returned no changes. Production creation plan/apply was `10 add, 0 change, 0 destroy`. A post-create provider convergence defect proposed replica replacement because inherited encryption/autoscaling were omitted from configuration; owner-approved declarations aligned code with the live resource and removed all actions. Final targeted plans returned no changes in both environments. Direct AWS checks confirm available PostgreSQL 16.13 `db.t4g.large` Multi-AZ encrypted/private primaries with seven-day backups and complete two-subnet groups; production also has an available encrypted `db.t4g.medium` read replica. | Production: 10 resources added, none changed/destroyed. Code: declared replica encryption and storage-autoscaling ceiling; added regression assertion. No replica replacement occurred. |
| 1.3.7 — ElastiCache Redis | Staging complete; production absent | Confirmed done | Confirmed done | ElastiCache tests passed (2/2). Staging targeted plan returned no changes. Production initial plan was `5 add, 0 change, 0 destroy`; the first apply safely stopped after creating subnet/parameter groups because the logs CMK policy omitted the actual `/{project}/{environment}/*` namespace. The scoped KMS correction passed tests and applied as one in-place key-policy update per account. Retry added the two encrypted log groups and replication group with no change/destroy. Final targeted plans returned no changes. AWS confirms available Redis 7.1 `cache.t4g.medium`, TLS, at-rest encryption, AUTH, and three-day snapshots in both accounts; production has two members with Multi-AZ and automatic failover enabled. | Production: five ElastiCache resources added across the initial/retry applies; no resource changed/destroyed. Logs KMS policies updated in place in both accounts. |
| 1.3.8 — SQS Queues | Staging complete; production absent | Confirmed done | Confirmed done | SQS module tests passed (3/3). Staging state contained all 51 scoped resources and its targeted plan returned no changes. Production's create-only plan and apply were `51 add, 0 change, 0 destroy`. Final targeted plans returned no changes in both environments. Direct SQS inspection found exactly 17 primary queues and 17 DLQs in each account, with a valid DLQ redrive policy and maximum-receive setting on every primary queue. | Production: 17 queues, 17 DLQs, and 17 redrive-allow policies added; no resource changed or destroyed. |
| 1.3.9 — IAM Roles (per service) | Staging roles/policies complete but its three existing ECR dependencies were outside state; production absent | Confirmed done | Confirmed done | IAM tests passed (6/6), including complete 17-service role coverage, authoritative ECR/log resources, pipeline permission boundaries, and no wildcard actions. Existing staging ECR repositories matched encryption/scanning/immutability requirements, were declaratively imported together, and received only standard tags (`3 import, 0 add, 3 change, 0 destroy`). Production's scoped apply was `71 add, 0 change, 0 destroy`: 17 task roles, 17 execution roles, 34 inline policies, and three required ECR repository dependencies. Final scoped plans returned no changes. Direct IAM authorization inventories confirmed 34 service roles, 34 attached inline policies, and zero wildcard actions in each account. | Staging: no IAM resource changed; three existing ECR dependencies imported and tagged. Production: 68 IAM resources and three ECR dependencies added; none changed/destroyed. GitHub application-CD roles remained out of scope for Task 1.3.13. |
| 1.3.10 — ECS Cluster | Staging complete; production absent | Confirmed done | Confirmed done | ECS module tests passed (4/4), including the observable Fargate cluster and Phase 1-only boundaries. Staging had both scoped cluster resources and a clean targeted plan. Production's cluster-only plan/apply was `2 add, 0 change, 0 destroy`. Final plans returned no changes. Direct ECS inspection confirmed both clusters ACTIVE, Container Insights enabled, `FARGATE` and `FARGATE_SPOT` associated, and the default strategy set to on-demand `FARGATE` with base 1/weight 1. | Production: ECS cluster and capacity-provider association added; no resource changed or destroyed. Task definitions/services remained out of scope for Task 1.3.15. |
| 1.3.11 — VPC Endpoints | Staging complete; production absent | Confirmed done | Confirmed done | VPC module test passed (1/1), asserting the full endpoint service set, private DNS, two-AZ interface placement, S3 gateway type/four route tables, and private-app-only HTTPS ingress. Staging contained all 14 scoped resources and had a clean plan. Production's plan/apply was `14 add, 0 change, 0 destroy`. Final plans returned no changes. Direct EC2 inspection found all 11 endpoints available in each VPC: ten interfaces with private DNS and two subnets each, plus one S3 gateway endpoint associated with four private route tables. | Production: endpoint security group, two ingress rules, ten interface endpoints, and one S3 gateway endpoint added; no resource changed or destroyed. |
| 1.3.12 — ECR | Existing staging repositories were outside Terraform state and lacked managed lifecycle/TLS policies; production repositories were created as Task 1.3.9 dependencies but also lacked those policies | Confirmed done | Confirmed done | ECR tests passed (3/3), covering the exact API/worker/frontend inventory, KMS encryption, immutable tags, scan-on-push, protected deletion, lifecycle retention, and TLS-only access. Task 1.3.9 safely imported and tagged the three matching staging repositories and created the three production repositories. Task 1.3.12 then applied identical create-only plans (`6 add, 0 change, 0 destroy`) in both accounts for lifecycle and TLS-deny policies. Final plans returned no changes. Direct ECR inspection confirmed all three repositories hardened, all three two-rule lifecycle policies active, and all three TLS-deny policies active in each account. | Both accounts: three lifecycle policies and three TLS-deny repository policies added; no resource changed/destroyed during this task. Staging import blocks retained for reconciliation provenance. |
| 1.3.13 — Application CD IAM Roles & GitHub Contract | Staging AWS roles/policies and variables existed but IAM was outside state; production application roles and variables were absent | Confirmed done | Confirmed done | IAM tests passed (6/6), including application role separation and least-privilege boundaries. Staging's exact existing role pair and policies were declaratively imported (`4 import, 0 add, 2 tag-only changes, 0 destroy`); production applied `4 add, 0 change, 0 destroy`. Final scoped plans returned no changes. GitHub's immutable repository/owner IDs match Terraform. Direct verification in both accounts confirmed exact environment/branch/workflow OIDC trust, zero wildcard actions, no ECS actions in build policies, and no ECR actions in deploy policies. All 15 Terraform-defined GitHub environment variables match exactly in both `staging` and `production`; JSON-valued variables were validated after preserving their quoting. | Staging: four existing IAM objects imported and two roles tagged; one subnet-list GitHub variable normalized. Production: two roles/two policies added and 14 missing GitHub variables configured (account ID already existed). No resource destroyed. |
| 1.3.14 — ALB & HTTPS | Staging edge existed outside Terraform state; production absent; shared DNS had no production access path | Confirmed done | Confirmed done | ALB tests passed (3/3). Owner approved the actual shared-zone production architecture. Global DNS now owns a least-privilege record-manager role trusted only by the production Terraform role and production AdministratorAccess SSO roles; production assume-role access and policy simulation succeeded. Staging applied `20 import, 1 ACM validation waiter add, 12 in-place alignments, 0 destroy`; production applied `21 add, 0 change, 0 destroy`. Final scoped plans returned no changes. Each state contains 21 ALB resources. Direct checks confirmed both ALBs active/internet-facing, two listeners, HTTP redirect, TLS 1.3/1.2 policy, issued ACM certificates, WAF associations, and access logging. The authoritative zone contains all four API/frontend aliases and four ACM validation records. | Global DNS: two IAM resources added and its policy minimally extended for provider read calls. Staging: existing edge imported/aligned without replacement. Production: complete 21-resource edge created. One conclusively stale lock from a failed network-resolution plan was removed by its exact lock ID after confirming no Terraform process existed. |

| 1.3.15 — ECS Task Definitions & Phase 1 Services | Staging API/frontend services and runtime logs existed outside Terraform state; production had no services, task definitions, runtime logs, or images | Confirmed done | Confirmed done | ECS module validation succeeded and tests passed (4/4), including exact Phase 1-only service inventory, immutable images, hardened task definitions, writable runtime paths, and the Application CD contract. Existing staging services/log groups were adopted without replacement; corrected task-definition revisions were registered while all prior revisions were preserved. Authorized Application CD run `31805877766` built immutable production images for commit `2baeb39e253c0f0cd95a7bedaba1eaae53d6dc52` with deployment disabled. Production created two KMS-encrypted log groups, API/frontend/migration task definitions, and exactly two services with no Phase 2 worker service. Focused startup corrections set API/migration `TMPDIR=/dev/shm` for non-root read-only containers and frontend `HOSTNAME=0.0.0.0`. Final scoped plans returned no changes in both accounts. Direct production checks confirmed API and frontend ACTIVE, rollout COMPLETED, desired/running `2/2`, zero pending, two healthy targets per target group, API `/health/live` returned `{"status":"alive"}`, frontend HTTPS returned 200, and the one-shot migration definition is ACTIVE but not a service. | Staging: four existing runtime resources imported/aligned and managed task-definition revisions registered; healthy services were not redeployed. Production: immutable images pushed, two log groups/three definitions/two services created, failed bootstrap revisions preserved, services recovered in place without deletion, and GitHub `ECS_BOOTSTRAP_IMAGE_TAG` set to the verified production commit SHA. No Phase 2 service or destructive resource action occurred. |

### Task commit messages

- 1.3.1: `docs(status): reconcile task 1.3.1 in staging and production`
- 1.3.2: `docs(status): reconcile task 1.3.2 security groups in both accounts`
- 1.3.3: `docs(status): reconcile task 1.3.3 KMS keys in both accounts`
- 1.3.4: `docs(status): reconcile task 1.3.4 S3 in both accounts`
- 1.3.5: `feat(infra): complete task 1.3.5 with Groq and Resend secrets`
- 1.3.6: `fix(infra): complete task 1.3.6 encrypted RDS replica convergence`
- 1.3.7: `fix(infra): complete task 1.3.7 Redis and logs KMS access`
- 1.3.8: `feat(infra): complete task 1.3.8 SQS topology in production`
- 1.3.9: `feat(infra): complete task 1.3.9 per-service IAM in both accounts`
- 1.3.10: `feat(infra): complete task 1.3.10 production ECS cluster`
- 1.3.11: `feat(infra): complete task 1.3.11 production VPC endpoints`
- 1.3.12: `feat(infra): complete task 1.3.12 hardened ECR repositories`
- 1.3.13: `feat(infra): complete task 1.3.13 application CD contract`
- 1.3.14: `feat(infra): complete task 1.3.14 shared-DNS ALB and HTTPS`
- 1.3.15: `fix(infra): complete task 1.3.15 Phase 1 ECS services`

## Stage 1.4 — Database Schema v2

| Task | Done condition met | Environments | Evidence | Files touched | Test result | Commit message |
|---|---|---|---|---|---|---|
| 1.4.1 — Configure Alembic | Yes | Staging database; repository configuration | Both live databases were confirmed without an `alembic_version` table before the override. The eight superseded pre-v2 revisions and their old-model tests were retired. One-shot staging ECS task `8b9d9ddc404744c399b340b6cc3ab9ec` ran `alembic current` against PostgreSQL with exit code 0 and transactional-DDL initialization; temporary task definition revision 8 was deregistered afterward. | `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/README`, `backend/alembic/versions/*` (pre-v2 removal), superseded migration tests, `backend/tests/unit/test_alembic_configuration.py`, `BUILD_STATUS.md` | Pass — 16 focused unit tests; Ruff clean; staging `alembic current` exit 0 | `feat(db): reset and configure Stage 1.4 v2 Alembic` |
| 1.4.2 — Migration 0001: Schema Namespaces | Yes | Staging and production | Preflight catalog probes returned no v2 schemas or tables in either database. Identical image digest `sha256:e9305ccc32a980e73708377142c4db6b4eb905017d68890c5b4c8f2852b4a1d3` applied revision `0001` to staging task `71255175b0ab4e5ba86261e6ceff0168` and production task `eeea67c8e1c64cfd8a1109b368100b36`, both exit 0. Independent verification tasks confirmed Alembic head `0001` and the exact eleven namespaces in each database. Temporary task definition revisions were deregistered. | `backend/alembic/versions/0001_2026_08_15_create_schema_namespaces.py`, `backend/tests/unit/test_schema_namespaces_migration.py`, `backend/tests/unit/test_alembic_configuration.py`, `BUILD_STATUS.md` | Pass — 19 focused unit/offline-migration tests; Ruff clean; both live verification tasks exit 0 | `feat(db): create v2 schema namespaces` |
